## Descripción
```
I've gotten bored of handing out flags as text. Wouldn't it be cool if they were an image instead?You can download the challenge files here:

- [challenge.zip](https://artifacts.picoctf.net/c_atlas/16/challenge.zip)

The same files are accessible via SSH here:`ssh -p 65156 ctf-player@atlas.picoctf.net`Using the password `6abf4a82`. Accept the fingerprint with `yes`, and `ls` once connected to begin. Remember, in a shell, passwords are hidden!
```
## Solución
```
Solo se tiene que descargar el archivo "challenge.zip" en la compu (no desde la consola), abrirlo después de descargarlo e ir abriendo las carpetas contenidas (challenge.zip\home\ctf-player\drop-in) en el .zip hasta llegar a uno que contenga una imagen llamada "flag". Al abrir la imagen dará un codigo QR, solo se escanea con alguna app o desde la camara del celular y dará la bandera

flag: picoCTF{p33k_@_b00_7843f77c}
```

## Notas 
```

```

## Referencias
````

```
